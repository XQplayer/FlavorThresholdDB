# Migration from `E:\codex\Aroma analysis`

The FlavorThresholdDB-specific source materials, OCR cache, extraction programs, generated indexes, QA report, and frontend entity-search implementation were consolidated into this repository on 2026-07-31.

The old project was not deleted during migration. This protects unrelated GC-MS and aroma-analysis work and provides a rollback source until the new repository has been used successfully.

Use `data/MIGRATION_MANIFEST.csv` to verify the migrated files by size and SHA-256. Future FlavorThresholdDB development should use this repository as the canonical location.

